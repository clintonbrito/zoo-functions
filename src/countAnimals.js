const data = require('../data/zoo_data');

// const { name, popularity } = data.species;
// const nameAndPopularity = `${name}: ${popularity}`;

const countAnimals = (animal) => {
  // if (animal === undefined) {
  //   return {
  //     ${name}: ${popularity}
  //   };
  // }
  // return nameAndPopularity;
};

module.exports = countAnimals;
